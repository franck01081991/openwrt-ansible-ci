config system
        option hostname '{{ system.hostname }}'
        option timezone '{{ system.timezone }}'
        option zonename '{{ system.zonename }}'

config timeserver 'ntp'
        option enabled '1'
{% for s in system.ntp_servers %}
        list server '{{ s }}'
{% endfor %}
